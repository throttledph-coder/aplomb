USER FLOW
Complete Journey — Free Tier
text

FIRST OPEN
    │
    ▼
Onboarding (2 steps)
  Step 1: Welcome screen → explain app
  Step 2: Connect AI → enter Groq key or skip (3 free tries before required)
    │
    ▼
Dashboard
    │
    ▼
Click "Start New Interview Session"
    │
    ▼
Step 1: Choose or upload resume
  → If no resume: Upload PDF or paste text
  → App parses resume, extracts: skills, experience, education, projects
  → Shows parsed preview: "We found these skills: React, Python, AWS..."
  → User confirms
    │
    ▼
Step 2: Enter job details
  → Job title (text input)
  → Company name (text input)
  → Interview type (Technical / Behavioral / Mixed)
  → Full job description (large textarea)
  → Click "Start Interview Session"
    │
    ▼
Background processing (shows progress bar):
  "Analyzing job description... (1/3)"
  "Matching your resume to the role... (2/3)"
  "Preparing your interview assistant... (3/3)"
    │
    ▼
Live Interview Screen
  → Timer starts
  → User types interviewer's question into input box
  → Clicks "Ask" or presses Enter
  → Answer generates (streams in word by word)
  → User reads answer, speaks it
  → Repeat for all questions
    │
    ▼
Click "End Session"
    │
    ▼
Post-Session Report
  → Summary
  → All Q&A pairs
  → Keyword matching
  → Coaching notes
    │
    ▼
Dashboard (session appears in history)
Complete Journey — Premium Tier
text

Everything above, PLUS:
    │
    ▼
Live Interview Screen
  → Auto-Listen toggle is ON by default
  → Microphone captures audio continuously
  → Whisper transcribes every 3 seconds
  → Smart filter runs on each chunk
  → When real question detected:
      - Question appears in "Heard:" box
      - User can confirm with [✓] or dismiss with [✗]
      - If confirmed (or after 2-second auto-confirm delay): AI generates answer
  → User can still type questions manually at any time
    │
    ▼
Stealth Toggle (top of screen)
  → Click to enable stealth mode
  → App disappears from screen share
  → Only visible as system tray icon to user
  → Small always-on-top overlay shows current answer
  → Global hotkey shows/hides overlay